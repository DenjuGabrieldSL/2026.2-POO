# Importa o módulo 'math' para usar operações matemáticas avançadas
import math

# Define a função 'Diagonal' recebendo base (b) e altura (h)
def Diagonal(b, h):
    # Aplica o Teorema de Pitágoras: raiz quadrada de (b² + h²)
    return math.sqrt(b**2 + h**2)